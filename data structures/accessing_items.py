letters = ['a', 'b', 'c', 'd', 'e', 'f']

# we can slice the list also using the colon it also has the step the third argument
print(letters[::-1])  # reversing the list


letters[0] = 'A'
print(letters)

# list in python are mutable , so we can update them
