letters = [1, 2, 3, 4, 5, 6]
const = letters.count(3)

# count method will return the number of characters in the list

# If a value is not present then it will throw a error called as ValueError

# index method will return the index of the first character

if 1 in letters:
    print(letters.index(1))
